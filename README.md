
# readme

This repository contains replication packages for working papers. Basically, Build a R project and run ```RUN.R``` to reproduce results. Before running, you will need to follow the instructions in the data directory to access the data. Your working directories should match the directory structure of this repository.
